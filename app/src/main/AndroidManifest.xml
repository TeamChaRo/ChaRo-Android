<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.charo_android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- 갤러리 권한 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChaRoAndroid"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.main.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.onBoarding.OnBoardingActivity">
        </activity>
        <activity android:name=".ui.write.WriteMapLocationActivity" />
        <activity android:name=".ui.write.WriteMapSearchActivity" />
        <activity android:name=".ui.search.ResultSearchActivity" />
        <activity android:name=".ui.search.SearchActivity" />
        <activity android:name=".ui.detail.DetailActivity" />
        <activity android:name=".ui.write.WriteMapActivity" />
        <activity android:name=".ui.write.WriteActivity"
            android:windowSoftInputMode="stateHidden|adjustResize"></activity>
        <activity
            android:name=".ui.search.NoSearchActivity"
            android:label="@string/title_activity_no_search"
            android:theme="@style/Theme.ChaRoAndroid.NoActionBar" />
        <activity android:name=".ui.signin.SignInActivity">
        </activity>
        <activity
            android:name=".ui.main.MainActivity"
            android:label="@string/app_name"></activity>
    </application>

</manifest>